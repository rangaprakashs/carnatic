# Cubase Carnatic Engineering Skill

**Owner:** rangaprakashs

## Overview
This repository contains the complete **Cubase‑Carnatic‑Engineering** skill for the Antigravity framework. It provides:
- Detailed workflows for tempo conversion, musical‑mode processing, and acoustic‑treatment advice.
- Scripts (`detect_cubase.py`, `search_cubase_pdfs.py`) for searching Cubase documentation.
- A `SKILL.md` file with front‑matter describing the skill, its purpose, and tag metadata.
- Reference materials and a knowledge‑base schema used by Antigravity.

## Karpathy‑style Knowledge‑Base Guidelines
- **Metadata**: All markdown files start with a YAML front‑matter block (`---`) containing `title`, `tags`, `description`, and `version`.
- **Tag taxonomy**: `audio`, `DAW`, `Cubase`, `Carnatic`, `workflow`, `automation`.
- **Linking**: Files reference each other with `file:///absolute/path` links so an AI can resolve symbols instantly.
- **Incremental updates**: New scripts or documentation are added as separate markdown files; the `README.md` serves as the entry point.

## Installation (Antigravity)
```sh
# Clone the repository
git clone https://github.com/rangaprakashs/cubase-carnatic-engineering.git

# Move it into the Antigravity plugins directory (optional if you already have it)
mv cubase-carnatic-engineering $HOME/.gemini/config/plugins/

# Activate the skill inside Antigravity (if not auto‑loaded)
antigravity enable-plugin cubase-carnatic-engineering
```

## Usage Example (AI Prompt)
```
You are an AI assistant. Use the `cubase-carnatic-engineering` skill to:
- Convert a 120 BPM vocal track to 80 BPM while preserving pitch.
- Apply Musical Mode to a set of events.
- Generate a room‑treatment checklist for a non‑sound‑proof studio.
```

## Contribute
Feel free to open Issues or PRs to improve workflows, add new scripts, or refine the knowledge‑base.

---
*Generated by Antigravity on 2026‑05‑27.*
